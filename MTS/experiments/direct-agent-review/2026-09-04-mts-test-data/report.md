# Отчёт предварительного ревью ТЗ

- Запуск: `2026-09-04-mts-test-data`
- Режим: `agent`
- Агент: `Codex`
- Модель: `GPT-5` / `unknown`
- Профиль: ТЗ на потоки и витрины данных — базовый профиль, версия 0.1.0
- Техническая проверка: `complete`. Она проверяет структуру и привязки, а не правильность выводов.

## Сводка

Проверены все три примера: поток геолокации, поток CDR и ежемесячная витрина-агрегат. Сформулировано 16 вопросов до передачи в разработку: 12 высокого и 4 среднего приоритета. Основные риски связаны с отсутствующей трассировкой полей и обязательных параметров источников, не до конца определённой временной логикой, противоречивой fallback-логикой региона, недетерминированным выбором последних записей и неясной семантикой обновления. Статус всех замечаний — unreviewed: решение об изменении ТЗ принимает человек.

## Охват

Прочитано фрагментов: 172 из 172.

## Источники

- `s001` (document): [Тестовые данные.pdf](sources/s001.pdf); SHA-256 `019f561d37b836e7aece1540144981b909046a34dd38cb300dba308eb137b426`; parser `pdfplumber 0.11.10`.
- `s002` (context): [Основные моменты документации.pdf](sources/s002.pdf); SHA-256 `14e18c54b1bfc9037af473f56f0ade53d473946642ac59c0a0fa1fa63ac6558e`; parser `pdfplumber 0.11.10`.
- `s003` (context): [Шаблоны документации.pdf](sources/s003.pdf); SHA-256 `a09bcbfb6c43439be468a4d9fa340fe39595f86e0e6d93f8312ec372328c578d`; parser `pdfplumber 0.11.10`.
- `s004` (context): [README.md](sources/s004.md); SHA-256 `500325066d3290442472d69a5820bda104ff62272e9e2c485f45afaa815fa554`; parser `utf8-text 1`.
- `s005` (context): [document-analysis.md](sources/s005.md); SHA-256 `ed721956716568c8f36d017d195aa622c90481338e040f5f02b87bcfcad53260`; parser `utf8-text 1`.

## Замечания

### F-002. Расположение источников и инфраструктурные параметры не определены

**Приоритет:** Высокий. Неоднозначное физическое расположение блокирует подключение источников и проверку маршрута данных.

**Проблема:** В потоке геолокации перечислены топики без Kafka-кластера и ссылок на каталог; в CDR-примере HDFS-адрес свёрнут до значения «Папка»; в витрине перечислены таблицы без прямых ссылок на Data Catalog.

**Почему:** Предоставленный чек-лист требует Kafka-кластер, прямую ссылку на Data Catalog и полный HDFS-путь с форматом. Без этих реквизитов разработчик не может однозначно найти физические источники и приёмники. Поскольку документы обезличены, требуется отделить заменённые значения от отсутствующей структуры.

**Вопрос:** Какие поля формы должны содержать Kafka-кластер, прямые ссылки на Data Catalog и полные HDFS-пути, и какие текущие заглушки действительно заменяют эти значения при обезличивании?

**Статус человека:** Не рассмотрено

Основания:

- [Тестовые данные.pdf, стр. 3](sources/s001.pdf): «PROVIDER_GEO → Kafka (24 часа хранения)» (`s001-p0003-text`).
- [Тестовые данные.pdf, стр. 5, таблица 1, строка 2](sources/s001.pdf): «Центр \| TOPIC_MSCP_CDR_CENTRAL \| Папка» (`s001-p0005-t001-r002`).
- [Тестовые данные.pdf, стр. 7, таблица 1, строка 2](sources/s001.pdf): «SCHEMA_RAW \| TABLE_IUM_RAW_PS» (`s001-p0007-t001-r002`).
- [Основные моменты документации.pdf, стр. 1](sources/s002.pdf): «обязательно указывается кластер Kafka» (`s002-p0001-text`).

### F-003. Нет полной спецификации сериализации входов и выходов

**Приоритет:** Высокий. Несогласованная схема сообщения может сделать поток технически несовместимым уже на границе систем.

**Проблема:** Для Kafka-потоков и большинства приёмников не задана модель сообщения: формат, схема, способ сериализации и десериализации. В CDR-примере указаны CSV/gzip и ORC только для отдельных участков, но не описано, как одна входная модель отображается в Kafka и два RAW-приёмника.

**Почему:** Без модели входного и выходного потока нельзя проверить совместимость producer/consumer, эволюцию схемы и корректность преобразования форматов.

**Вопрос:** Каковы формат, версия схемы и сериализация/десериализация для каждого Kafka-топика и приёмника, включая переход CSV/gzip → Kafka → ORC/CSV?

**Статус человека:** Не рассмотрено

Проверенная область ТЗ:

- `s001-p0003-text`
- `s001-p0005-text`
- `s001-p0006-text`
- `s001-p0007-text`

### F-004. Для полей витрин не указана обязательность

**Приоритет:** Высокий. Правило непосредственно влияет на состав строк, агрегаты и контракт витрины.

**Проблема:** Структура TABLE_AGG_DEVICES содержит имя, тип и описание пяти выходных полей, но не фиксирует NOT NULL/NULLABLE и поведение при неизвестном регионе, неизвестном вендоре или отсутствии подходящего IMEI.

**Почему:** Предоставленный чек-лист требует признак обязательности для каждого поля витрины. Без него реализация и тесты могут по-разному трактовать строки, для которых справочник или входное значение отсутствуют.

**Вопрос:** Какие поля TABLE_AGG_DEVICES обязательны, допускаются ли NULL или технические значения UNKNOWN, и должна ли строка исключаться при невозможности определить регион или вендора?

**Статус человека:** Не рассмотрено

Проверенная область ТЗ:

- `s001-p0008-text`
- `s001-p0008-t001-r002`
- `s001-p0008-t001-r003`
- `s001-p0008-t001-r004`
- `s001-p0008-t001-r005`
- `s001-p0008-t001-r006`

### F-005. Не описан алгоритм потока геолокации между слоями

**Приоритет:** Высокий. Отсутствует центральная логика реализации нескольких заявленных приёмников.

**Проблема:** Документ перечисляет маршрут PROVIDER_GEO → Kafka → Flink → RAW → DDS → ADDS → CDM и пять таблиц-приёмников, но структура приведена только для RAW 3G/4G, а пошаговые фильтрации, преобразования, обогащения и правила формирования DDS/ADDS/CDM отсутствуют.

**Почему:** Названия слоёв и общая ссылка на алгоритмы не определяют вычисления и границы ответственности этапов. Разработчик не сможет восстановить выходные поля, а тестировщик — сформировать ожидаемый результат.

**Вопрос:** Какие операции и правила перехода выполняются на каждом участке RAW → DDS → ADDS → CDM, какие поля формируются и какие случаи отбрасываются?

**Статус человека:** Не рассмотрено

Проверенная область ТЗ:

- `s001-p0002-text`
- `s001-p0003-text`
- `s001-p0004-text`

### F-006. Два требования к задержке имеют разную, но не определённую семантику

**Приоритет:** Высокий. Неопределённый SLO нельзя однозначно реализовать и проверить под нагрузкой.

**Проблема:** На одной странице одновременно заданы «Задержка: < 1 мин» и «Стриминг в реальном времени (задержка ≈ 0 сек)», а также часовые партиции. Не указаны начало и конец измерения, процентиль и связь потоковой доступности с партиционированием.

**Почему:** Разные команды могут реализовать и проверять latency как задержку доставки в Kafka, обработки во Flink или доступности в конкретном слое, получив несовместимые критерии приёмки.

**Вопрос:** Для какого участка и показателя задан каждый предел, какой процентиль и окно измерения использовать, и когда событие считается доступным потребителю?

**Статус человека:** Не рассмотрено

Основания:

- [Тестовые данные.pdf, стр. 2](sources/s001.pdf): «Задержка: < 1 мин» (`s001-p0002-text`).
- [Тестовые данные.pdf, стр. 2](sources/s001.pdf): «Стриминг в реальном времени (задержка ≈ 0 сек)» (`s001-p0002-text`).
- [Тестовые данные.pdf, стр. 2](sources/s001.pdf): «Инкрементная загрузка по часовым партициям» (`s001-p0002-text`).

### F-007. Не определены поля и крайние случаи удаления лидирующих нулей

**Приоритет:** Высокий. Правило изменяет ключевые значения и способно нарушить соединения и идентичность абонента.

**Проблема:** Шаг преобразования ссылается на «эти поля», но непосредственно перед правилом список полей отсутствует. Не определено поведение для пустой строки, NULL, строки из одних нулей, знака и нечисловых символов.

**Почему:** Разные реализации могут преобразовать идентификаторы по-разному, потерять значимые нули или получить разные значения на крайних случаях.

**Вопрос:** К каким именно полям применяется очистка и каков ожидаемый результат для NULL, пустой строки, `0`, строки из нескольких нулей, знаковых и нечисловых значений?

**Статус человека:** Не рассмотрено

Основания:

- [Тестовые данные.pdf, стр. 5](sources/s001.pdf): «Из этих полей удаляются лидирующие нули до первого значимого символа» (`s001-p0005-text`).

### F-008. Фильтрация CDR и коррекция времени не связаны в одно правило

**Приоритет:** Высокий. Неоднозначность влияет на полноту данных и воспроизводимость повторных загрузок.

**Проблема:** Окно от −7 дней до +2 часов задано относительно времени обработки, но не названо поле события и точка фиксации текущего времени. Для регионов заданы сдвиги 7 и 3 часа, при этом не указаны исходный/целевой часовой пояс, направление сдвига, поля применения и связь с FIELD_TIME_ZONE_SHIFT и FIELD_TIMEZONE_CALC.

**Почему:** Порядок нормализации времени и фильтрации меняет набор принятых событий, особенно на границах окна и при повторном запуске.

**Вопрос:** По какому timestamp выполняется фильтрация, в какой зоне и до/после какой коррекции; что является `current time`; как региональное правило соотносится с FIELD_TIME_ZONE_SHIFT и FIELD_TIMEZONE_CALC?

**Статус человека:** Не рассмотрено

Основания:

- [Тестовые данные.pdf, стр. 5](sources/s001.pdf): «не ранее 7 дней назад и не позже 2 часов вперёд от текущего времени» (`s001-p0005-text`).
- [Тестовые данные.pdf, стр. 5](sources/s001.pdf): «Коррекция часового пояса для временных меток» (`s001-p0005-text`).
- [Тестовые данные.pdf, стр. 6, таблица 1, строка 13](sources/s001.pdf): «FIELD_TIME_ZONE_SHIFT \| string \| Сдвиг часового пояса» (`s001-p0006-t001-r013`).
- [Тестовые данные.pdf, стр. 6, таблица 1, строка 14](sources/s001.pdf): «FIELD_TIMEZONE_CALC \| int \| Вычисленный сдвиг в минутах» (`s001-p0006-t001-r014`).

### F-010. Нет трассировки входных полей витрины к таблицам-источникам

**Приоритет:** Высокий. Отсутствие lineage блокирует реализацию центрального расчёта витрины.

**Проблема:** Алгоритм TABLE_AGG_DEVICES использует FIELD_IMSI, FIELD_IMEI, FIELD_LAC, FIELD_CELL_ID, FIELD_TIME_STAMP, FIELD_CELL_GEN и tac, но документ не показывает, в какой исходной таблице находится каждое поле, его тип, обязательность и преобразование до выходных полей.

**Почему:** Перечня таблиц и формул недостаточно для однозначной реализации соединений между двумя фактами и тремя справочниками; шаблон предусматривает построчное соответствие атрибутов источника и приёмника.

**Вопрос:** Для каждого входного и выходного атрибута укажите таблицу-источник, исходное поле, тип, условие соединения и преобразование; как объединяются FIELD_IMSI из RAW_MS и RAW_PS?

**Статус человека:** Не рассмотрено

Проверенная область ТЗ:

- `s001-p0007-text`
- `s001-p0008-text`
- `s001-p0009-text`

### F-011. Предварительный отбор региона исключает описанные fallback-ветви

**Приоритет:** Высокий. Противоречие напрямую меняет региональную атрибуцию и итоговую меру.

**Проблема:** Сначала выбирается последняя запись с ненулевыми FIELD_LAC и FIELD_CELL_ID, а затем предписываются соединения для случаев `lac = 0` и `cell = 0`. При буквальном выполнении первого условия такие строки не дойдут до fallback.

**Почему:** Два допустимых порядка дают разные регионы и разное число абонентов в агрегате.

**Вопрос:** Fallback применяется к последней записи до отбора по ненулевым значениям, после неуспешного полного `(lac, cell_id)`-соединения или по другому правилу; что делать, если оба значения равны 0 или NULL?

**Статус человека:** Не рассмотрено

Основания:

- [Тестовые данные.pdf, стр. 8](sources/s001.pdf): «последняя запись с ненулевыми FIELD_LAC и FIELD_CELL_ID» (`s001-p0008-text`).
- [Тестовые данные.pdf, стр. 9](sources/s001.pdf): «если lac = 0 → джойн только по cell_id (4G)» (`s001-p0009-text`).
- [Тестовые данные.pdf, стр. 9](sources/s001.pdf): «если cell = 0 → джойн только по lac» (`s001-p0009-text`).

### F-012. Выбор последнего значения и устранение дублей недетерминированы

**Приоритет:** Высокий. Недетерминированность меняет измерения агрегата и осложняет повторную проверку результата.

**Проблема:** Для IMEI сказано только «последний по времени», без имени timestamp и разрешения равенств. Для региона после соединения требуется одна region_code на абонента, но не задано, как выбирать строку при нескольких совпадениях справочника, пересекающихся периодах действия или равных временных метках.

**Почему:** На одинаковом наборе данных разные движки или планы выполнения могут выбрать разные IMEI/регион, из-за чего результат перестаёт быть воспроизводимым.

**Вопрос:** Какой timestamp и полный порядок сортировки определяют последний IMEI и регион; какие даты актуальности и кардинальности имеют справочники; какое правило разрешает несколько совпадений и равные timestamps?

**Статус человека:** Не рассмотрено

Основания:

- [Тестовые данные.pdf, стр. 8](sources/s001.pdf): «FIELD_IMEI (последний по времени за месяц)» (`s001-p0008-text`).
- [Тестовые данные.pdf, стр. 9](sources/s001.pdf): «Убираются дубли: на выходе — одна region_code на абонента» (`s001-p0009-text`).

### F-013. Агрегатная мера включена в список полей группировки

**Приоритет:** Высокий. Ошибка в grain меняет число строк и значения основной меры витрины.

**Проблема:** FIELD_USERS_CNT = count(distinct FIELD_IMSI) перечислено внутри раздела «Группировка по» вместе с FIELD_REGION_NAME и FIELD_VENDOR_NAME.

**Почему:** Мера не может одновременно вычисляться агрегатной функцией и служить исходным ключом группировки в том же уровне расчёта; буквальная реализация отличается от вероятного намерения.

**Вопрос:** Верно ли, что ключ группировки состоит из FIELD_REGION_NAME, FIELD_VENDOR_NAME и, вероятно, FIELD_BIZ_DATE, а FIELD_USERS_CNT вычисляется после группировки как count(distinct FIELD_IMSI)?

**Статус человека:** Не рассмотрено

Основания:

- [Тестовые данные.pdf, стр. 9](sources/s001.pdf): «Группировка по» (`s001-p0009-text`).
- [Тестовые данные.pdf, стр. 9](sources/s001.pdf): «FIELD_USERS_CNT = count(distinct FIELD_IMSI)» (`s001-p0009-text`).

### F-014. «Инкремент» не связан с полной перезагрузкой месяца

**Приоритет:** Высокий. Правило влияет на целостность партиций и восстановление после сбоев.

**Проблема:** Способ загрузки назван «Инкремент», а требование к обновлению — «Только полная перезагрузка месяца (без upsert)». Не определено, инкрементально ли добавляются новые месяцы с заменой выбранной партиции, какие месяцы пересчитываются и как учитываются поздние события.

**Почему:** Без точной семантики перезапуска нельзя гарантировать идемпотентность, полноту истории и отсутствие дублей при сбое или backfill.

**Вопрос:** Как выбирается месяц для расчёта и перезаписи, удаляется ли партиция до записи, какие late-arriving данные вызывают перерасчёт, как выполняются retry и исторический backfill?

**Статус человека:** Не рассмотрено

Основания:

- [Тестовые данные.pdf, стр. 7](sources/s001.pdf): «Способ загрузки
Инкремент» (`s001-p0007-text`).
- [Тестовые данные.pdf, стр. 9, таблица 1, строка 8](sources/s001.pdf): «Только полная перезагрузка месяца
(без upsert)» (`s001-p0009-t001-r008`).

### F-001. Структура шаблона не сохранена явно

**Приоритет:** Средний. Пробел затрудняет контроль полноты, но часть разделов может быть неприменима и не обязательно меняет расчёт.

**Проблема:** Во всех трёх примерах отсутствуют отдельные разделы шаблона, при этом документ не помечает их как «не применимо». Например, не выделены Data Catalog, источники обогащения, пример данных, DDL и история изменений; состав пропусков различается между примерами.

**Почему:** По предоставленному чек-листу все разделы шаблона должны сохраняться, а неприменимые этапы — маркироваться. Иначе читатель не отличает осознанное отсутствие от незаполненного требования.

**Вопрос:** Какие отсутствующие разделы нужно восстановить в каждом из трёх примеров, а какие явно отметить «не применимо»?

**Статус человека:** Не рассмотрено

Проверенная область ТЗ:

- `s001-p0002-text`
- `s001-p0003-text`
- `s001-p0004-text`
- `s001-p0005-text`
- `s001-p0006-text`
- `s001-p0007-text`
- `s001-p0008-text`
- `s001-p0009-text`

### F-009. FAQ опирается на поля вне описанной структуры CDR

**Приоритет:** Средний. Пробел блокирует только заявленную FAQ-логику, но не обязательно основной RAW-поток.

**Проблема:** Определение технологии связи использует radioAccessTechnology, CELL_ID и LAC, но ни одного из этих полей нет в заявленной структуре TABLE_MSCP_RAW_KFK из 15 полей.

**Почему:** Неясно, доступны ли поля в исходном CDR, вычисляются, приходят из другого набора или FAQ относится к другому контракту. Реализовать правило только по данному документу невозможно.

**Вопрос:** В каких источниках и таблицах определены radioAccessTechnology, CELL_ID и LAC, как они связываются с TABLE_MSCP_RAW_KFK и входит ли вычисление технологии связи в границы этого ТЗ?

**Статус человека:** Не рассмотрено

Основания:

- [Тестовые данные.pdf, стр. 6](sources/s001.pdf): «(из Kafka, ORC-формат, 15 полей)» (`s001-p0006-text`).
- [Тестовые данные.pdf, стр. 6](sources/s001.pdf): «по полю radioAccessTechnology» (`s001-p0006-text`).
- [Тестовые данные.pdf, стр. 6](sources/s001.pdf): «например, CELL_ID, LAC» (`s001-p0006-text`).

### F-015. Не заданы объёмы данных для CDR и витрины

**Приоритет:** Средний. Пробел не меняет бизнес-формулу, но препятствует оценке производительности и ёмкости.

**Проблема:** В CDR-примере и витрине-агрегате нет ожидаемого среднего и пикового объёма входа, месячного числа уникальных IMSI, размера партиции и прогнозируемого роста. Геолокационный пример задаёт throughput, но также не связывает его с объёмом хранения по слоям.

**Почему:** Объём данных указан среди приоритетов проверки и в шаблоне нефункциональных требований. Без него нельзя обосновать ресурсы, окно расчёта, SLA и реалистичный нагрузочный тест.

**Вопрос:** Каковы средний/пиковый поток, суточный и месячный объём, число уникальных ключей, размер партиций, срок хранения и ожидаемый рост для каждого примера?

**Статус человека:** Не рассмотрено

Проверенная область ТЗ:

- `s001-p0002-text`
- `s001-p0005-text`
- `s001-p0007-text`
- `s001-p0009-text`

### F-016. Нет проверяемых условий приёмки и сверки результатов

**Приоритет:** Средний. Пробел повышает риск позднего обнаружения ошибок, хотя часть критериев может храниться во внешнем тестовом артефакте.

**Проблема:** Для трёх примеров не заданы контрольные входы, ожидаемые выходы, допустимые расхождения и инварианты: сохранность/отсев записей по этапам, уникальность ключа, число пользователей до и после соединений, поведение при пустом месяце и повторном запуске.

**Почему:** Описание алгоритма без наблюдаемых критериев не позволяет тестировщику отличить корректную реализацию от правдоподобного, но неверного результата.

**Вопрос:** Какие минимальные контрольные примеры и инварианты должны пройти приёмку для каждого потока и витрины, включая границы времени, NULL, дубли, отсутствие справочника, пустой период и повторный запуск?

**Статус человека:** Не рассмотрено

Проверенная область ТЗ:

- `s001-p0002-text`
- `s001-p0003-text`
- `s001-p0004-text`
- `s001-p0005-text`
- `s001-p0006-text`
- `s001-p0007-text`
- `s001-p0008-text`
- `s001-p0009-text`

## Ограничения

- Документы «Основные моменты документации.pdf» и «Шаблоны документации.pdf» использованы как предоставленный нормативный контекст, но их актуальная версия и формальный статус в MTS не подтверждены.
- Исходный PDF прямо сообщает об обезличивании. Поэтому значения USER_*, LINK_*, CLUSTER и «Папка» не признаны ошибками сами по себе; вопросы относятся к наличию и смыслу обязательных реквизитов.
- В наборе нет реальных строк данных, DDL, схем сериализации, карточек Data Catalog, истории изменений и эталонных результатов, поэтому типы, кардинальности, форматы и исполнимость соединений нельзя подтвердить фактически.
- Три примера уже использовались в предыдущем локальном анализе и подключённом контексте; этот отчёт не является независимым измерением качества будущего решения на контрольном документе.
- Все замечания требуют решения автора или профильного эксперта и не подтверждают фактический дефект реализации или экономический эффект продукта.
